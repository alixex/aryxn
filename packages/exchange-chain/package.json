{
  "name": "@alixex/exchange-chain",
  "version": "1.0.0",
  "description": "Unified Exchange SDK for Swap and Bridge operations",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm",
    "dev": "tsup src/index.ts --format cjs,esm --watch",
    "lint": "oxlint",
    "type-check": "tsc -b --noEmit"
  },
  "dependencies": {
    "@alixex/chain-constants": "workspace:*",
    "@alixex/cross-chain": "workspace:*",
    "@alixex/query-chain": "workspace:*",
    "@alixex/swap-multichain": "workspace:*",
    "@alixex/wallet-core": "workspace:*"
  }
}
